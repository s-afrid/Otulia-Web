{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd server && npm install --legacy-peer-deps && cd .. && cd client && npm install --legacy-peer-deps && npm run build"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}